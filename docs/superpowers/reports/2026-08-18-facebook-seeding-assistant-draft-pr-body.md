# Draft PR Body Reference

## Summary

Adds a Facebook-first Seeding Assistant to ACP with operator-supplied target URLs, template-first content generation, deterministic risk gates, hybrid auto-submit/review, global/shift pause, single-submit verification, KPI reporting, and a minimal-permission Chrome MV3 extension.

## Verification

Sandbox: domain/schema/extension/report/manager harness checks pass as documented in the verification report. Full Flask/release/live-DOM gates remain for Ubuntu and are explicitly not claimed as passing.
